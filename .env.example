# Path to the configuration file
COCO_RAG_CONFIG="./config.yml"

# Database credentials (used by docker-compose and the application)
COCO_RAG_DB=cocorag
COCO_RAG_DB_USER=cocorag
COCO_RAG_DB_PASSWORD=<your-password>

# Postgres connection URL for CocoIndex (must match credentials above)
COCOINDEX_DATABASE_URL=postgres://cocorag:<your-password>@localhost:6432/cocorag

# CocoIndex Rust Logging Level
# Suppress all sqlx logging including slow query warnings
# Note: sqlx slow query warnings are hardcoded at Info level in cocoindex
# Setting sqlx=warn will suppress Info-level slow query warnings
RUST_LOG=warn,sqlx=warn

# CocoIndex Database Connection Pool Settings
# Maximum number of database connections (default: 25)
# Reduce this if your database has connection limits or becomes unstable
COCOINDEX_DATABASE_MAX_CONNECTIONS=10
# Minimum number of database connections (default: 5)
COCOINDEX_DATABASE_MIN_CONNECTIONS=2

# CocoIndex Global Concurrency Settings
# These limits apply across all flows and sources to prevent database overload

# Maximum number of concurrent rows being processed (default: 1024)
# Lower values reduce memory usage and database pressure
# Start with 128-256 and increase if system is stable
COCOINDEX_SOURCE_MAX_INFLIGHT_ROWS=128

# Maximum memory footprint of data in flight (default: None/unlimited)
# Set to limit memory usage (example: 100MB = 104857600 bytes)
# COCOINDEX_SOURCE_MAX_INFLIGHT_BYTES=104857600

# Reranking settings (optional overrides)
# COCO_RAG_RERANKING_ENABLED=true
# COCO_RAG_RERANKING_TYPE=auto
